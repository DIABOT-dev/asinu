name: ASINU CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: asinu-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.18.1"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run type-check

      - name: i18n check
        run: npm run i18n:check

      - name: Asset reference check
        run: npm run lint:assets

      - name: ESLint
        run: npm run lint

      # This is the same JavaScript bundle validation that EAS runs before
      # the native iOS build. It catches Expo Router import incompatibilities
      # in CI instead of waiting for a remote EAS build.
      - name: Expo iOS bundle check
        env:
          EXPO_PUBLIC_APP_ENV: staging
          EXPO_PUBLIC_API_BASE_URL: https://asinu.top
          NODE_ENV: production
        run: |
          export_dir="${RUNNER_TEMP}/asinu-ios-export"
          node ./node_modules/expo/bin/cli export \
            --platform ios \
            --output-dir "$export_dir"
