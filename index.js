// Expo Router is the application's entry point. Keeping a legacy `App` import
// here makes Metro resolve `./App` before it reads `package.json#main`; this
// project has no `App` component and therefore crashes the development bundle.
import 'expo-router/entry';
